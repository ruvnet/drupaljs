[package]
name = "drupaljs-uuid"
version = "0.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

# NOTE: this crate is a member of the root `crates/*` workspace and inherits
# `[workspace.package]` / `[workspace.dependencies]` from `../../Cargo.toml`.

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen.workspace = true
